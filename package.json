{
  "name": "nextjsapp",
  "private": true,
  "workspaces": [
    "app"
  ],
  "devDependencies": {
    "turbo": "^1.6.3"
  },
  "scripts": {
    "start": "turbo run start --scope=app",
    "build": "turbo run build --scope=app --include-dependencies"
  }
}
