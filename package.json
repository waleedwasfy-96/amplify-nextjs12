{
  "name": "nextjsapp",
  "private": true,
  "workspaces": [
    "app",
    "common"
  ],
  "devDependencies": {
    "turbo": "^1.6.3"
  },
  "scripts": {
    "build": "turbo run build --scope=app --include-dependencies"
  }
}
