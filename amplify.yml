version: 1
applications:
  - appRoot: app
    frontend:
      phases:
        preBuild:
          commands:
            - cd ..
            - yarn install
        build:
          commands:
            - yarn build
            - ls -la
            - cd app
#            - cp next.config.js .next/standalone
#            - cp -R public .next/standalone/public
#            - cp -R node_modules .next/standalone/node_modules
            - ls -la
            - pwd
      artifacts:
        baseDirectory: app/.next/standalone/.next
        files:
          - '**/*'
      cache:
        paths:
          - node_modules/**/*
