{
  "name": "nextjsapp",
  "private": true,
  "workspaces": [
    "app",
    "common"
  ],
  "dependencies": {
    "next": "13.0.2"
  },
  "devDependencies": {
    "turbo": "^1.6.3"
  },
  "scripts": {
    "build": "turbo run build --scope=app --include-dependencies"
  }
}
