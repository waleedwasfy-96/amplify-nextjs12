{
  "name": "nextjsapp",
  "private": true,
  "workspaces": [
    "app"
  ],
  "dependencies": {
    "next": "12.3.4"
  },
  "devDependencies": {
    "turbo": "^1.6.3"
  },
  "scripts": {
    "build": "turbo run build --scope=app --include-dependencies"
  }
}
